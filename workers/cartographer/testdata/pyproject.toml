[tool.poetry]
name = "my-project"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.100.0"
pydantic = "^2.0"

[tool.poetry.dev-dependencies]
pytest = "^7.0"
black = "^23.0"
